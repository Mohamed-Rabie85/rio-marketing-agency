# RIO Marketing Solutions - Setup & Deployment Guide

## 📋 نظرة عامة على المشروع

موقع احترافي لوكالة **RIO Marketing Solutions** مبني باستخدام **Next.js** (React 19 + Tailwind CSS 4 + Express 4 + tRPC 11) مع دعم كامل للغة العربية والاستجابة على جميع الأجهزة.

---

## 🚀 البدء السريع

### المتطلبات الأساسية:
- **Node.js** 18+ 
- **pnpm** (مدير الحزم الموصى به)
- **Git** (لرفع المشروع على GitHub)

### خطوات الإعداد المحلي:

```bash
# 1. استخراج ملفات المشروع
unzip rio-marketing-agency.zip
cd rio-marketing-agency

# 2. تثبيت المتطلبات
pnpm install

# 3. إعداد متغيرات البيئة
# انسخ ملف .env.example إلى .env (إن وجد) وأضف المتغيرات المطلوبة

# 4. تشغيل خادم التطوير
pnpm dev

# 5. افتح المتصفح على http://localhost:3000
```

---

## 📁 هيكل المشروع

```
rio-marketing-agency/
├── client/                    # كود React الأمامي
│   ├── src/
│   │   ├── pages/            # الصفحات الرئيسية (Home, Services, About, etc.)
│   │   ├── components/       # المكونات القابلة لإعادة الاستخدام
│   │   ├── lib/              # المكتبات والأدوات
│   │   ├── contexts/         # React Contexts
│   │   ├── hooks/            # Custom Hooks
│   │   ├── App.tsx           # المكون الرئيسي
│   │   ├── main.tsx          # نقطة الدخول
│   │   └── index.css         # الأنماط العامة
│   └── public/               # الملفات الثابتة
├── server/                    # كود Express الخلفي
│   ├── routers.ts            # إجراءات tRPC
│   ├── db.ts                 # دوال قاعدة البيانات
│   ├── storage.ts            # وظائف S3
│   ├── _core/                # الأساسيات (OAuth, Context, etc.)
│   ├── auth.logout.test.ts   # اختبارات تسجيل الخروج
│   └── features.test.ts      # اختبارات الميزات
├── drizzle/                   # قاعدة البيانات والترحيلات
│   ├── schema.ts             # تعريف الجداول
│   └── migrations/           # ملفات الترحيل
├── shared/                    # الأنواع والثوابت المشتركة
├── package.json              # المتطلبات والبرامج النصية
├── tsconfig.json             # إعدادات TypeScript
├── vite.config.ts            # إعدادات Vite
└── vitest.config.ts          # إعدادات الاختبارات
```

---

## 🛠️ الأوامر المتاحة

```bash
# تشغيل خادم التطوير
pnpm dev

# بناء المشروع للإنتاج
pnpm build

# تشغيل المشروع المبني
pnpm start

# التحقق من أخطاء TypeScript
pnpm check

# تشغيل الاختبارات
pnpm test

# تنسيق الكود
pnpm format

# دفع تغييرات قاعدة البيانات
pnpm db:push
```

---

## 📝 تحديث المحتوى

### تحديث الصفحات الرئيسية:

**الصفحة الرئيسية:** `client/src/pages/Home.tsx`
- غير النصوص والصور والأزرار
- حدث رسالة البطل والفلسفة

**صفحة الخدمات:** `client/src/pages/Services.tsx`
- أضف أو عدل الخدمات والأسعار
- حدث الوصفات والميزات

**صفحة من نحن:** `client/src/pages/About.tsx`
- حدث المعلومات عن الشركة والقيم
- أضف الميثاق الاستراتيجي

**صفحة الاتصال:** `client/src/pages/Contact.tsx`
- حدث معلومات الاتصال والبريد الإلكتروني
- عدل رقم الهاتف والساعات

### إضافة مقالات المدونة:

المقالات تُدار من خلال قاعدة البيانات. استخدم لوحة التحكم أو أضف مباشرة عبر tRPC.

### تحديث معلومات المؤسس:

**صفحة المؤسس:** `client/src/pages/Founder.tsx`
- أضف صورة احترافية
- حدث السيرة الذاتية
- أضف روابط وسائل التواصل الاجتماعي

---

## 🎨 التخصيص والتصميم

### تغيير الألوان:

عدل متغيرات CSS في `client/src/index.css`:

```css
@theme {
  --color-primary: #10b981;      /* الأخضر الأساسي */
  --color-secondary: #0d9488;    /* التيل */
  --color-accent: #f59e0b;       /* الذهبي */
}
```

### تغيير الخطوط:

عدل استيراد الخطوط في `client/index.html`:

```html
<link href="https://fonts.googleapis.com/css2?family=Cairo:wght@400;600;700&display=swap" rel="stylesheet">
```

### تخصيص الشعار:

استبدل الشعار في `client/src/components/Navigation.tsx` و `client/src/components/Footer.tsx`

---

## 🔐 متغيرات البيئة

المتغيرات التالية مُدارة تلقائياً من قبل Manus:

```
DATABASE_URL=              # اتصال قاعدة البيانات
JWT_SECRET=                # سر التوقيع
VITE_APP_ID=               # معرف التطبيق
OAUTH_SERVER_URL=          # خادم OAuth
VITE_OAUTH_PORTAL_URL=     # بوابة تسجيل الدخول
BUILT_IN_FORGE_API_KEY=    # مفتاح API المدمج
BUILT_IN_FORGE_API_URL=    # رابط API المدمج
```

---

## 📊 الميزات الرئيسية

### ✅ المميزات المطبقة:

1. **صفحات متعددة اللغات:**
   - دعم كامل للعربية مع RTL
   - تصميم استجابي

2. **نظام الاتصال والحجز:**
   - نموذج اتصال مع التحقق
   - نظام حجز جلسات استشارية
   - إشعارات فورية للمالك

3. **نظام المدونة:**
   - إنشاء وإدارة المقالات
   - دعم Markdown
   - نظام التصنيفات

4. **دراسات الحالة:**
   - عرض المشاريع السابقة
   - النتائج والإحصائيات
   - شهادات العملاء

5. **الأمان والمصادقة:**
   - Manus OAuth
   - جلسات آمنة
   - حماية البيانات

---

## 🚀 النشر على GitHub

### خطوات رفع المشروع:

```bash
# 1. إنشاء مستودع جديد على GitHub
# اذهب إلى https://github.com/new وأنشئ مستودع جديد

# 2. تهيئة Git محلياً
cd rio-marketing-agency
git init
git add .
git commit -m "Initial commit: RIO Marketing Solutions website"

# 3. إضافة الفرع الرئيسي وربطه
git branch -M main
git remote add origin https://github.com/YOUR_USERNAME/rio-marketing-agency.git
git push -u origin main
```

### ملف .gitignore (مُضمن بالفعل):

```
node_modules/
dist/
.env
.env.local
.DS_Store
```

---

## 🌐 النشر على Vercel

### الطريقة الأولى: من خلال Manus Dashboard

1. انقر على زر **"Publish"** في لوحة تحكم Manus
2. اختر **"Vercel"** كخيار الاستضافة
3. اتبع التعليمات

### الطريقة الثانية: من خلال Vercel مباشرة

```bash
# 1. ثبت Vercel CLI
npm i -g vercel

# 2. نشر المشروع
vercel

# 3. اتبع التعليمات التفاعلية
```

### الطريقة الثالثة: ربط GitHub مع Vercel

1. اذهب إلى https://vercel.com
2. انقر على **"New Project"**
3. اختر مستودع GitHub الخاص بك
4. انقر على **"Deploy"**

---

## 📱 الاختبار والجودة

### تشغيل الاختبارات:

```bash
# تشغيل جميع الاختبارات
pnpm test

# تشغيل اختبار محدد
pnpm test -- server/features.test.ts

# مراقبة التغييرات
pnpm test -- --watch
```

### التحقق من الأخطاء:

```bash
# التحقق من TypeScript
pnpm check

# تنسيق الكود
pnpm format
```

---

## 🔄 تحديث قاعدة البيانات

### إضافة جدول جديد:

1. عدّل `drizzle/schema.ts`
2. أضف الجدول الجديد
3. شغّل:

```bash
pnpm db:push
```

### إضافة بيانات أولية:

أنشئ ملف `seed.mjs` في جذر المشروع:

```javascript
import { getDb } from './server/db.ts';

const db = await getDb();
// أضف بياناتك هنا
```

ثم شغّل:

```bash
node seed.mjs
```

---

## 🐛 استكشاف الأخطاء

### المشكلة: الخادم لا يبدأ

```bash
# تأكد من تثبيت المتطلبات
pnpm install

# امسح الذاكرة المؤقتة
rm -rf node_modules
pnpm install

# أعد تشغيل الخادم
pnpm dev
```

### المشكلة: أخطاء قاعدة البيانات

```bash
# تحقق من اتصال قاعدة البيانات
pnpm db:push

# تحقق من المتغيرات البيئية
echo $DATABASE_URL
```

### المشكلة: أخطاء TypeScript

```bash
# تحقق من الأخطاء
pnpm check

# أصلح الأخطاء تلقائياً
pnpm format
```

---

## 📞 الدعم والمساعدة

للمزيد من المعلومات:

- **Manus Documentation:** https://docs.manus.im
- **React Documentation:** https://react.dev
- **Tailwind CSS:** https://tailwindcss.com
- **tRPC Documentation:** https://trpc.io

---

## 📄 الترخيص

هذا المشروع مرخص تحت MIT License.

---

**آخر تحديث:** ديسمبر 2024
**الإصدار:** 1.0.0
